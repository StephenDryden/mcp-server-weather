{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and Run MCP Weather Server",
            "type": "shell",
            "command": "make build && docker run --rm -i mcp-weather-server",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}